aside.span3.sidebar
  .sidebar-widget.well
    == slim :newsletter_signup_sidebar
  .sidebar-widget.well
    h2.sidebar-header
      | Popular posts
    ul.unstyled
      - popular_posts.each do |article|
        li
          h4
            a href="/blog/#{article.page_slug}"
              == article.title
          p
            | by 
            = article.author
